# DevSwitchboard v0.2 Upstream-First Execution Implementation Plan

> **Execution boundary:** Execute sequentially in this Codex context. Implementation subagents are prohibited. One fresh non-authoring review context is required after preliminary verification.

**Goal:** Implement the approved v0.2 Upstream-First Execution contract delta and record Dogfood #017 without changing historical v0.1 artifacts, authority, routing, or mandatory confidence semantics.

**Architecture:** Extend the existing Routing Recommendation and Approved Handoff schema families with version-conditioned `0.1` and `0.2` branches. Keep common fields in each canonical schema, validate version-specific structure in JSON Schema, and reserve repository-dependent source resolution and cross-field authority checks for `scripts/verify.mjs`. Extend Dogfood Record `0.1` compatibly with optional Codex-value checkpoints.

**Tech stack:** Markdown, JSON Schema Draft 2020-12, dependency-free Node.js verifier and regression harness, Git.

**Authority:** [`docs/north-star.md`](../../north-star.md), [`docs/superpowers/specs/2026-08-25-devswitchboard-v0.2-upstream-first-execution.md`](../specs/2026-08-25-devswitchboard-v0.2-upstream-first-execution.md), and Developer-approved task `devswitchboard-upstream-preparation-boundary-017` Revision 1.

## Preflight and execution constraints

- Baseline, refreshed `origin/main`, and isolated-worktree `HEAD`: `dc8e727ef0c02f3fb7f2b150effa327c87192336`.
- Branch: `codex/dogfood-017-upstream-preparation-boundary`.
- Worktree: `D:\me\devswitchboard-worktrees\dogfood-017`.
- Preflight outcome: `compatible`; no material local delta, conflict, route invalidation, strategy invalidation, or intent-feasibility conflict.
- Reuse upstream requirements, design, specification, contract versions, Dogfood protocol, and strategy. Do not reconstruct them.
- Do not modify the North Star, approved v0.2 specification, historical examples, historical Dogfood artifacts, routing rules, Task Profile, lifecycle contracts, or publication state.
- Do not commit, push, open a pull request, merge, tag, release, package, publish Pages, or delete the branch/worktree.

## Upstream grounding

| # | Approved logical work unit | Result | Local evidence and exact targets |
| --- | --- | --- | --- |
| 1 | Version-conditioned Routing Recommendation `0.1`/`0.2` and surface compatibility | `reused` | `schemas/routing-recommendation.schema.json` is the single canonical schema and the local validator supports `oneOf`, `const`, arrays, and closed objects. Add schema branches; exercise them through `tests/verify-regressions.mjs`; expose valid records in `examples/routing-recommendation-v0.2-{chat,developer,codex}.json`. |
| 2 | Version-conditioned Approved Handoff `0.1`/`0.2` and prepared/not-needed shapes | `reused` | `schemas/approved-handoff.schema.json` is the single canonical handoff schema and already centralizes shared `$defs`. Add version branches and an `upstream_preparation` definition; cover both valid states in `examples/approved-handoff-v0.2-{prepared,not-needed}.json`. |
| 3 | Resolvable provenance and Developer-authority preservation | `adapted` | JSON Schema can validate the source field shape but cannot test the live filesystem. Add schema shape checks plus repository-relative/shared-URL resolution in `scripts/verify.mjs`; retain and generalize the existing ready-handoff authority invariant across every handoff. No artifact registry is introduced. |
| 4 | Human contracts, Chat/Codex workflows, and Superpowers adapter | `reused` | Update `docs/contracts/README.md`, `docs/contracts/routing-recommendation.md`, `docs/contracts/approved-handoff.md`, `docs/workflows/chat.md`, `docs/workflows/codex.md`, and `docs/adapters/superpowers.md`. Preserve R006 planning, R012 direct execution, mandatory preflight, and fresh final verification. |
| 5 | Minimum canonical v0.2 fixtures | `adapted` | Existing routing examples are `routing_case` `0.1` wrappers and must remain historical. Add five standalone v0.2 fixtures and index them in `examples/README.md`; do not migrate the three existing routing cases or the existing handoff example. |
| 6 | Regression-first coverage and historical compatibility | `reused` | Extend `tests/verify-regressions.mjs` with real temporary-repository records that drive `scripts/verify.mjs`. First observe pre-feature rejection of valid v0.2 records, then implement GREEN validation. Preserve all current regression cases. |
| 7 | Optional Dogfood Codex-value measurement | `reused` | Extend `schemas/dogfood-record.schema.json` with optional `measurements.codex_value_checks`, following the existing optional `decision_value_checks` pattern. Add valid and malformed regression cases without changing the `0.1` schema version or historical records. |
| 8 | Execute and record Dogfood #017 | `reused` | Create `dogfood/devswitchboard-upstream-preparation-boundary-017.json` from observed execution evidence and add one entry to `dogfood/README.md`. Do not claim an A/B result or fabricated savings. |
| 9 | Independent review, remediation, and fresh verification | `reused` | After preliminary GREEN verification, dispatch exactly one read-only non-authoring reviewer. Record findings/remediations in Dogfood #017 and rerun every final check after the last evidence edit. |

No upstream work unit is `invalidated`. The two adaptations are repository-detail mappings and preserve intent, scope, acceptance, route, and strategy; both have impact `repository_adaptation`.

## Compatibility baseline and protected surfaces

- Historical routing inventory: three nested Routing Recommendations, all schema version `0.1`, in the existing `routing_case` examples.
- Historical Approved Handoff inventory: three records, all schema version `0.1`.
- Historical Dogfood inventory: 34 JSON files, all parsed successfully before mutation; Dogfood Record schema remains `0.1`.
- No historical Routing Recommendation or Approved Handoff contains `surface_value` or `upstream_preparation`.
- Canonical schema selection is determined by `schema` through `canonicalSchemaFiles`, with six existing path pins in `pinnedSchemaFiles`; new standalone fixtures use canonical schema identity and need no new bridge type.
- Protect byte content for every baseline file under `dogfood/` except the permitted `dogfood/README.md` index edit; after implementation, `git diff` against the baseline may add only Dogfood #017 and modify the index.
- Protect all baseline files in `examples/`; the v0.2 examples are additions only.
- Protect `docs/routing/rules.md`, all Task Profile and lifecycle schemas/contracts/examples, the North Star, the approved v0.2 specification, and all historical Superpowers plans/specifications.
- If any historical artifact must be rewritten, stop with a conflict report to the Developer.

## RED/GREEN tasks

### Task 1: Capture compatibility evidence and add RED routing tests

**Files:**
- Modify: `tests/verify-regressions.mjs`
- Inspect/protect: `examples/*.json`, `dogfood/*.json`, `schemas/routing-case.schema.json`

- [x] Save an external baseline hash manifest for every historical `dogfood/` file and record parsed schema/version counts.
- [x] Add literal valid v0.2 Chat, Developer, and Codex Routing Recommendation builders.
- [x] Add acceptance cases for one and multiple compatible Codex values.
- [x] Add rejection cases for missing/empty/unknown/cross-surface values and a v0.2-only field on `0.1`.
- [x] Run the focused regression suite and observe valid v0.2 records fail because the pre-feature schema accepts only `0.1`/rejects `surface_value`.

### Task 2: Add RED handoff and Dogfood measurement tests

**Files:**
- Modify: `tests/verify-regressions.mjs`

- [x] Add literal valid prepared and not-needed v0.2 handoff builders derived from the existing complete `0.1` example.
- [x] Add rejection cases for missing preparation, contradictory state content, missing provenance/summary/work unit/local question/rationale, unresolvable or display-only source, and contradictory ready authority.
- [x] Add acceptance/rejection cases proving historical `0.1` handoffs remain valid and reject `upstream_preparation`.
- [x] Add valid and malformed `codex_value_checks` cases covering every required field, classification vocabulary, nullable `upstream_item`, evidence, and unexpected properties.
- [x] Run the focused regression suite and record the intended pre-feature failures.

### Task 3: Implement machine contracts and semantic validation

**Files:**
- Modify: `schemas/routing-recommendation.schema.json`
- Modify: `schemas/approved-handoff.schema.json`
- Modify: `schemas/dogfood-record.schema.json`
- Modify: `scripts/verify.mjs`

- [x] Add closed version-conditioned Routing Recommendation branches and surface-specific vocabularies.
- [x] Add closed version-conditioned Approved Handoff branches and mutually exclusive prepared/not-needed definitions.
- [x] Add optional closed Codex-value checkpoint items to Dogfood Record `0.1`.
- [x] Add repository-independent URL/path classification and repository-relative source resolution for v0.2 prepared sources.
- [x] Apply ready-handoff authority checks to every Approved Handoff and reject preparation that attempts to contradict binding scope/strategy authority using only approved explicit invariants.
- [x] Add new canonical structures to the verifier without changing routing rules or lifecycle semantics.
- [x] Run the new cases GREEN, then run the complete existing regression suite and repository verifier.

### Task 4: Align human contracts and workflows

**Files:**
- Modify: `docs/contracts/README.md`
- Modify: `docs/contracts/routing-recommendation.md`
- Modify: `docs/contracts/approved-handoff.md`
- Modify: `docs/workflows/chat.md`
- Modify: `docs/workflows/codex.md`
- Modify: `docs/adapters/superpowers.md`

- [x] Document independent product/contract versions and historical `0.1` readability.
- [x] Define `surface_value` as advisory surface justification, not authorization.
- [x] Define the authority and mutually exclusive state semantics of `upstream_preparation`.
- [x] Add Chat's preparation-value decision and smallest sufficient transfer bundle.
- [x] Add Codex grounding classifications and material-evidence re-evaluation rule while retaining preflight.
- [x] Map preparation to Superpowers `writing-plans` only for selected repository-grounded planning and retain plan-free R012 direct execution.
- [x] Run Markdown-link, terminology, routing, authority, and unresolved-marker checks.

### Task 5: Add canonical v0.2 fixtures

**Files:**
- Create: `examples/routing-recommendation-v0.2-chat.json`
- Create: `examples/routing-recommendation-v0.2-developer.json`
- Create: `examples/routing-recommendation-v0.2-codex.json`
- Create: `examples/approved-handoff-v0.2-prepared.json`
- Create: `examples/approved-handoff-v0.2-not-needed.json`
- Modify: `examples/README.md`

- [x] Populate fixtures with complete canonical paths and approved authority semantics.
- [x] Ensure the prepared handoff references resolvable repository paths and the not-needed handoff uses R012-compatible bounded execution.
- [x] Validate all five fixtures through canonical schema selection and full verification.
- [x] Confirm the eight historical routing/handoff example surfaces remain byte-identical.

### Task 6: Execute Dogfood #017 and obtain independent review

**Files:**
- Create: `dogfood/devswitchboard-upstream-preparation-boundary-017.json`
- Modify: `dogfood/README.md`
- Modify: this plan

- [x] Record material checkpoints only: upstream preparation reuse; schema/verifier/file grounding; repository adaptations; implementation execution; independent review; mandatory final verification. Use only the approved six classifications.
- [x] Keep `DUPLICATED_REASONING` truthful; if present, record cause, remediate an in-scope defect, and rerun affected checks.
- [x] Keep Dogfood result/review/verification incomplete until their evidence actually exists.
- [x] Run preliminary full verification.
- [x] Dispatch exactly one fresh non-authoring reviewer with the canonical authority, bootstrap handoff, this plan, and complete diff.
- [x] Classify findings as Critical, Important, Minor, or Note; remediate every applicable boundary finding and re-review material remediation. Three Important findings across initial review and re-review were reproduced and remediated; final independent re-review passed with no unresolved finding.
- [x] Finalize Dogfood #017 and this plan only after review evidence exists.

### Task 7: Fresh final verification and handoff

**Files:**
- Verify: all changed and untracked files plus every repository JSON/schema-backed artifact

- [x] Run `node tests/verify-regressions.mjs` after the last content edit.
- [x] Run `node scripts/verify.mjs` after the last content edit.
- [x] Parse every repository JSON independently and report the exact count.
- [x] Confirm canonical schema selection and validation for historical `0.1` and new `0.2` records.
- [x] Run Markdown-link, terminology, routing, authority, decision-value, Codex-value, source-resolution, and unresolved-marker checks.
- [x] Compare the historical Dogfood hash manifest and protected v0.1 surfaces to the baseline.
- [x] Run `git diff --check` plus direct trailing-whitespace/end-of-file validation for every changed or untracked file.
- [x] Require the exact final inventory below, with no other repository file.
- [x] Return control to the Developer without publication.

## Exact final inventory expectation

```text
docs/adapters/superpowers.md
docs/contracts/README.md
docs/contracts/approved-handoff.md
docs/contracts/routing-recommendation.md
docs/superpowers/plans/2026-08-26-v0.2-upstream-first-execution.md
docs/workflows/chat.md
docs/workflows/codex.md
dogfood/README.md
dogfood/devswitchboard-upstream-preparation-boundary-017.json
examples/README.md
examples/approved-handoff-v0.2-not-needed.json
examples/approved-handoff-v0.2-prepared.json
examples/routing-recommendation-v0.2-chat.json
examples/routing-recommendation-v0.2-codex.json
examples/routing-recommendation-v0.2-developer.json
schemas/approved-handoff.schema.json
schemas/dogfood-record.schema.json
schemas/routing-recommendation.schema.json
scripts/verify.mjs
tests/verify-regressions.mjs
```

Any required second surface outside this inventory is a stop condition unless it is only an evidence-preserving plan/Dogfood correction already listed above.

## Dogfood #017 checkpoint design

| Checkpoint | Phase | Expected classification | Evidence boundary |
| --- | --- | --- | --- |
| `CV-017-001` | `codex_preflight` | `LOCAL_GROUNDING_REQUIRED` | Exact baseline, workspace, toolchain, schema, verifier, fixture, and historical inventories come from live repository truth. |
| `CV-017-002` | `implementation_planning` | `UPSTREAM_PREPARATION_REUSED` | The nine approved logical units are mapped without reopening approach selection. |
| `CV-017-003` | `implementation_planning` | `LOCAL_GROUNDING_REQUIRED` | Exact schema branching, verifier placement, fixture paths, and validation commands come from live files. |
| `CV-017-004` | `implementation_planning` | `REQUIRED_ADAPTATION` | Filesystem source resolution is split between schema shape and semantic verification. |
| `CV-017-005` | `implementation_planning` | `REQUIRED_ADAPTATION` | Existing `0.1` wrapper examples remain historical; new standalone v0.2 fixtures are added. |
| `CV-017-006` | `implementation` | `IMPLEMENTATION_EXECUTION` | Regression, schema, verifier, documentation, adapter, and fixture changes are applied. |
| `CV-017-007` | `verification` | `MANDATORY_CONFIDENCE` | Preliminary regressions and repository verification establish review-ready evidence. |
| `CV-017-008` | `review` | `MANDATORY_CONFIDENCE` | Fresh independent non-authoring review challenges value classifications and boundary semantics. |
| `CV-017-009` | `implementation` | `IMPLEMENTATION_EXECUTION` | Confirmed authority and provenance findings are remediated through regression-first verifier changes. |
| `CV-017-010` | `review` | `MANDATORY_CONFIDENCE` | Remediation re-review verifies fixes and challenges false positives. |
| `CV-017-011` | `implementation` | `IMPLEMENTATION_EXECUTION` | A confirmed implementation-versus-review subagent false positive is reproduced and corrected. |
| `CV-017-012` | `review` | `MANDATORY_CONFIDENCE` | Final independent re-review confirms all remediation with no unresolved finding. |
| `CV-017-013` | `verification` | `MANDATORY_CONFIDENCE` | Fresh final regressions, verifier, parsing, schema, link, semantic, inventory, history, and whitespace checks run after the last change. |

Additional checkpoints are added only for an actual material activity. No checkpoint claims savings, and no A/B implementation is planned.

## Revision 2 continuation — completion ownership and cross-task commit verification

On 2026-08-26, after the Revision 1 local checkpoint and its final review and verification had completed, the Developer clarified two binding workflow decisions:

- Codex technical completion returns to Chat for technical acceptance and remediation routing; it does not require Developer inspection of implementation diffs, source code, tests, verifier output, technical findings, or technical remediation.
- Every Approved Handoff `0.2` must record the explicit Boolean `developer_decisions.chat_verify_commit_before_next_task`. Dogfood #017 selects `true`.

This is a mid-task `DEVELOPER_INTENT_CLARIFICATION`. It does not invalidate the active route or approved strategy, require Re-route Required or Conflict Report, or authorize a restart. The exact existing 20-file checkpoint, original grounding, RED/GREEN evidence, and three remediated Important review findings above remain historical execution evidence and are reused without reconstruction.

### Continuation preflight and grounding

```yaml
outcome: compatible_with_adaptation
route_invalidated: false
strategy_invalidated: false
restart_required: false
adaptations:
  - Apply the approved Revision 2 completion-routing policy.
  - Add the explicit cross-task commit-verification decision.
conflicts: []
next_action: continue_existing_route
```

| Revision 2 work | Result | Local evidence and exact targets |
| --- | --- | --- |
| Require the explicit per-task Boolean while preserving `0.1` | `adapted` | Extend the existing version-conditioned branch in `schemas/approved-handoff.schema.json`; update `scripts/verify.mjs`; demonstrate `true` and `false` through `examples/approved-handoff-v0.2-{prepared,not-needed}.json`; reject omission, non-Boolean values, and the v0.2-only field on `0.1` in `tests/verify-regressions.mjs`. Impact: `repository_adaptation`. |
| Route technical completion to Chat | `reused` | Align `docs/contracts/approved-handoff.md`, `docs/workflows/chat.md`, `docs/workflows/codex.md`, and `docs/adapters/superpowers.md`; preserve mandatory verification and Developer ownership of material intent, policy, strategy, overrides, merge, and publication. |
| Block a dependent next-task handoff on the `true` path | `adapted` | Chat owns the pre-emission block. An emitted `predecessor_commit_verification` gate is validated in `scripts/verify.mjs` and records predecessor task ID, exact 40-hex commit SHA, and a resolvable evidence source. The `false` path creates no mandatory audit gate. Impact: `repository_adaptation`. |
| Persist the approved design delta | `reused` | Advance `docs/superpowers/specs/2026-08-25-devswitchboard-v0.2-upstream-first-execution.md` from Revision 1 to Revision 2 without changing its North Star, routing, lifecycle, or version-boundary decisions. |
| Preserve and extend actual Dogfood evidence | `reused` | Advance `dogfood/devswitchboard-upstream-preparation-boundary-017.json` to task revision 2 while preserving Revision 1 checkpoints, findings, and verification history. Record that no stable remote commit, Chat acceptance, or predecessor audit exists yet. |

No prior upstream work unit becomes invalidated. The Revision 2 changes add focused policy semantics to the already selected Approved Handoff family and completion workflow; they do not create a bridge, phase, routing rule, Task Profile dimension, or authority surface.

### Focused Revision 2 RED/GREEN sequence

- [x] RED: a valid Approved Handoff `0.2` with policy `true` was rejected before the field existed in the closed schema.
- [x] RED: a valid Approved Handoff `0.2` with policy `false` was rejected before the field existed in the closed schema.
- [x] RED: the pre-policy verifier accepted a missing policy field.
- [x] RED: the pre-policy verifier did not produce the approved non-Boolean policy diagnostic.
- [x] RED: the pre-policy verifier did not produce the approved v0.1-only-field diagnostic.
- [x] RED: pre-policy workflow guidance still returned technical completion directly to the Developer and lacked the three Chat acceptance outcomes.
- [x] RED: pre-policy workflow guidance did not block a true-policy dependent handoff pending exact stable-commit audit and did not protect the false path from a fabricated mandatory gate.
- [x] RED: pre-policy workflow guidance did not explicitly state that Developer technical code/test review is unnecessary while preserving Developer-owned decisions.
- [x] RED: a follow-up exact-commit case rejected a valid stable GitHub commit URL before the verifier could bind evidence to the recorded SHA.
- [x] GREEN: the complete regression suite passed after the smallest schema, semantic-verifier, contract, workflow, adapter, example, specification, and Dogfood changes, including exact-SHA commit-evidence matching, while retaining every Revision 1 regression.

The predecessor gate is not claimed for the current uncommitted checkpoint. On the `true` path, Chat MUST withhold a dependent next-task Approved Handoff until a stable remote commit exists and Chat has verified that exact commit. When emitted, the next handoff gate uses `gate: predecessor_commit_verification`, status `passed` or `reused`, and machine-readable predecessor task ID and commit SHA plus resolvable `evidence_source`. On the `false` path, omission of that gate is valid unless a separate material event gives inspection decision value.

### Revision 2 review and final evidence

- Run one fresh non-authoring review after the complete Revision 2 delta is present. The reviewer specifically challenges completion ownership, both Boolean choices, omission/non-Boolean rejection, `0.1` compatibility, true/false predecessor-gate behavior, exact-SHA evidence, current non-claims, Developer authority, and regression of any previously remediated finding.
- Record only actual material continuation activities in Dogfood #017. Preserve `DUPLICATED_REASONING: 0` unless live evidence proves otherwise.
- Rerun the complete regression suite, repository verifier, JSON/schema/link/terminology/authority/value/source/history/inventory checks, `git diff --check`, and direct whitespace checks after the specification, this continuation, Dogfood Revision 2, review evidence, and all remediations are final.
- Codex completion returns `READY_FOR_CHAT_ACCEPTANCE` to Chat. This local task does not commit, push, open or merge a pull request, tag, release, publish, delete the branch/worktree, or begin a dependent task.

The updated final inventory expectation is the existing 20-file checkpoint plus exactly:

```text
docs/superpowers/specs/2026-08-25-devswitchboard-v0.2-upstream-first-execution.md
```

No other new path is expected. The future stable commit, remote push, Chat commit audit, and `predecessor_commit_verification` gate are post-checkpoint actions requiring separate publication authority.

### Revision 2 independent review result

The fresh non-authoring reviewer returned `pass` after the complete Revision 2 continuation. It found no Critical, Important, Minor, or boundary-relevant issue; independently reran the regression suite, repository verifier, and `git diff --check`; confirmed the exact 21-file inventory and historical immutability; verified that none of the three Revision 1 remediations regressed; and found no duplicated material reasoning. No remediation was required. Chat acceptance, predecessor commit audit, dependent-task authorization, and publication remain unclaimed.

### Revision 2 fresh final verification result

After review evidence and all continuation content were finalized, the complete regression suite and repository verifier passed. All 67 repository JSON files parsed independently; all 54 example and Dogfood artifacts passed canonical schema selection; the verifier passed Markdown links, terminology, routing, authority, decision-value, Codex-value, completion-policy, source-resolution, and unresolved-marker checks; the exact inventory was 21 files; 37 historical Dogfood files and 14 historical example files matched `origin/main`; and `git diff --check` plus direct trailing-whitespace/final-newline checks passed. These checks are rerun after this plan and the Dogfood record contain the final evidence, so the completion claim does not rely on a pre-evidence tree.
