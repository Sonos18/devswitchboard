# DevSwitchboard v0.1 Release Exit Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to execute this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Implementation subagents are prohibited; Stage 1 permits exactly one fresh read-only release-audit context.

**Goal:** Audit whether the frozen DevSwitchboard v0.1 repository is internally consistent, artifact-only usable, release-hygienic, and ready for Chat's release-exit evaluation without adding semantic capability.

**Architecture:** Treat commit `560f98a04da0807be664bd3c5c28fa282f88e9b8` as the immutable semantic baseline. Stage 1 gathers repository, verification, clean-room, recovery, Dogfood, and GitHub release evidence; this plan is its only repository artifact. Any later Stage 2 work is conditional on Chat's classification and is limited to release-exit evidence/index artifacts unless separately approved remediation changes that route.

**Tech Stack:** Markdown and JSON artifacts, JSON Schema Draft 2020-12, dependency-free Node.js verification, Git/GitHub read-only inspection.

**Spec:** Developer-approved Stage 1 directive for `devswitchboard-v0.1-release-exit-016`.

## Global Constraints

- v0.1 semantic development is frozen. Do not add a missing product capability to make the release appear complete.
- During Stage 1, modify only `docs/superpowers/plans/2026-08-25-v0.1-release-exit.md`.
- Do not create Dogfood #016 or modify `dogfood/README.md` before Chat's release-exit evaluation.
- Do not modify README, normative documents, schemas, examples, verifier, regressions, historical Dogfoods, or release metadata.
- Do not use implementation subagents. Use one fresh non-authoring context only for artifact-only first-run, recovery, and independent release review.
- Do not create commits, pushes, pull requests, tags, releases, packages, Pages, or other publication.
- Classify findings as `CRITICAL`, `IMPORTANT`, `MINOR`, or `NOTE`. A missing new semantic capability is `BLOCKED`, not implementation authority.
- Return exactly one release decision: `PASS`, `PASS_WITH_MINIMAL_REMEDIATION_REQUIRED`, or `BLOCKED`.
- The permitted plan must remain visible to all baseline checks and must not hide a verification failure.

---

## Planned File and Evidence Boundaries

| Surface | Audit responsibility |
| --- | --- |
| `README.md`, `CONTRIBUTING.md`, `LICENSE`, root tree | Public positioning, product boundary, contribution and license agreement, accidental runtime/package surface. |
| `docs/superpowers/specs/2026-08-20-devswitchboard-v0.1-design.md` | Approved Revision 2 intent, in/out boundaries, principles, phases, contracts, recovery, measurement, and acceptance. |
| `docs/getting-started.md`, `docs/workflows/*.md`, `docs/adapters/superpowers.md`, `docs/state-and-recovery.md`, `docs/routing/rules.md` | First-run clarity, phase routing, adapter mapping, efficiency invariants, pause/resume behavior. |
| `docs/contracts/*.md`, `schemas/*.schema.json`, `examples/*.json` | Human contract, machine schema, canonical example consistency. |
| `dogfood/*.json`, `dogfood/README.md` | Historical evidence, conditional optional-field compatibility, recovered blockers, review and verification findings. |
| Git refs, tags, GitHub PRs/releases | Shared baseline, open work, release identity, and publication hygiene. |
| `docs/superpowers/plans/2026-08-25-v0.1-release-exit.md` | Sole Stage 1 repository artifact and release-audit checkpoint. |

### Task 1: Establish exact isolated preflight

**Files:**
- Inspect: Git worktree, branch, `origin/main`, repository instructions, root toolchain
- Create: `docs/superpowers/plans/2026-08-25-v0.1-release-exit.md`

**Interfaces:**
- Consumes: required baseline and Stage 1 freeze directive.
- Produces: a clean, isolated, no-delta audit workspace and this plan.

- [x] **Step 1: Create the isolated worktree at the exact baseline.**

Use branch `codex/dogfood-016-v0.1-release-exit` at `560f98a04da0807be664bd3c5c28fa282f88e9b8`.

- [x] **Step 2: Run Codex Preflight.**

Confirm `HEAD`, `origin/main`, and merge-base equal the required baseline; HEAD/origin trees are identical; the worktree is clean; no repository-local `AGENTS.md` exists; Node is available; and `git diff HEAD origin/main` is empty.

- [x] **Step 3: Preserve this plan as the only Stage 1 artifact.**

After creation, every later inventory check must report exactly this path and no other repository edit.

### Task 2: Run complete baseline verification with the plan present

**Files:**
- Verify: all repository files, including this untracked plan

**Interfaces:**
- Consumes: exact isolated baseline plus permitted plan.
- Produces: fresh workflow, schema, link, terminology, routing, decision-value, marker, and whitespace evidence.

- [x] **Step 1: Run the workflow-integrity suite.**

Run `node tests/verify-regressions.mjs` and require exit code zero with `workflow-integrity regressions: PASS`.

- [x] **Step 2: Run the complete repository verifier.**

Run `node scripts/verify.mjs` and require every named category to pass, including JSON syntax, schema conformance, semantic invariants, Markdown links, terminology, routing rules, unresolved markers, decision value, and verification.

- [x] **Step 3: Run independent supporting checks.**

Parse every repository JSON file, confirm every schema-backed artifact has a matching applicable schema result through the verifier, run the canonical unresolved-marker scan with its two documented historical exclusions, and run `git diff --check` plus direct whitespace validation for the permitted untracked plan.

- [x] **Step 4: Confirm exact Stage 1 inventory.**

Require `git status --porcelain` to contain only `docs/superpowers/plans/2026-08-25-v0.1-release-exit.md`.

### Task 3: Audit product boundary and routing/efficiency invariants

**Files:**
- Inspect: `README.md`
- Inspect: `CONTRIBUTING.md`
- Inspect: `docs/superpowers/specs/2026-08-20-devswitchboard-v0.1-design.md`
- Inspect: `docs/getting-started.md`
- Inspect: `docs/workflows/chat.md`
- Inspect: `docs/workflows/codex.md`
- Inspect: `docs/adapters/superpowers.md`
- Inspect: `docs/routing/rules.md`
- Inspect: root directory and all public guidance found by focused searches

**Interfaces:**
- Consumes: approved v0.1 intent and all public route guidance.
- Produces: product-boundary and routing/efficiency finding lists with severity.

- [x] **Step 1: Confirm the affirmative v0.1 boundary.**

Cross-check documentation-first/manual-first operation, advisory recommendations, developer final authority, deterministic next-incomplete-phase routing, exactly seven Task Profile dimensions, `chat|codex|developer` surfaces, Superpowers-only adapter support, and explicit bridge/recovery behavior.

- [x] **Step 2: Confirm excluded product surfaces remain absent.**

Inspect the root tree and focused repository searches for CLI, editor/platform plugin, web application, multi-provider integration, autonomous enforcement, learned/opaque routing, package/build/runtime manifests, generated outputs, and binaries. Distinguish explanatory non-goal mentions from actual product surfaces.

- [x] **Step 3: Confirm routing and efficiency invariants.**

Trace next-incomplete-phase routing; `REMOTE|LOCAL|INTENT` context sources; `MORE_CONTEXT_REQUIRED|NO_MORE_CONTEXT_NEEDED`; semantic-gate reuse; non-deduplicable final verification; relevant/irrelevant/`UNKNOWN` divergence; profile refresh versus route invalidation; two-part route/strategy continuation; optional eligibility versus selection; qualified lower-orchestration ties; mandatory confidence work; and developer approval.

### Task 4: Audit every contract, schema, and canonical example

**Files:**
- Inspect: `docs/contracts/*.md`
- Inspect: `schemas/*.schema.json`
- Inspect: `examples/*.json`
- Inspect: `examples/README.md`
- Inspect: verifier schema-to-artifact routing in `scripts/verify.mjs`

**Interfaces:**
- Consumes: canonical contract index, human definitions, schemas, examples, and verifier mappings.
- Produces: a complete contract matrix for Task Profile, Routing Recommendation, Approved Handoff, Local Delta, both Micro Consultation directions, Codex Preflight, Conflict Report, Re-route Required, Work State, Verification Report, and Dogfood record.

- [x] **Step 1: Build the contract matrix from repository facts.**

For each named contract, record the human contract path, schema path or explicit absence, example paths, canonical schema/name/version, core enums, authority/provenance fields, lifecycle states, and required fields. Treat the Dogfood record as a canonical measured-run contract even though it lives outside `docs/contracts/`.

- [x] **Step 2: Compare human, machine, example, and verifier semantics.**

Use schema parsing plus focused inspection to confirm names, enums, authority, revisions, lifecycle states, conditional fields, and required properties. Record any mismatch with severity rather than repairing it.

### Task 5: Audit recovery and Dogfood #001-#015

**Files:**
- Inspect: `docs/state-and-recovery.md`
- Inspect: `docs/contracts/work-state.md`
- Inspect: `docs/contracts/re-route-required.md`
- Inspect: `docs/contracts/conflict-report.md`
- Inspect: recovery examples and canonical Dogfood recovery/checkpoint artifacts
- Inspect: all `dogfood/*.json` and `dogfood/README.md`

**Interfaces:**
- Consumes: artifact-only lifecycle semantics and all historical measured evidence.
- Produces: recovery/provenance and Dogfood compatibility findings.

- [x] **Step 1: Trace verification-failure recovery.**

Confirm the recorded verification-failure evidence, `verification_failed` Work State, stale-evidence rule, successful rerun, and safe completion transition are reconstructable from artifacts.

- [x] **Step 2: Trace Re-route Required and Conflict Report recovery.**

Confirm checkpoint preservation, pause state, exact referenced artifact, same-task/revision authority, unique highest applicable Approved Handoff, supersession, historical evidence retention, and active-blocker release without hidden conversation.

- [x] **Step 3: Audit all historical Dogfood records.**

Require all records to remain schema-valid; classify historical failures/checkpoints as evidence after valid supersession; preserve optional-field backward compatibility; require machine-readable decision-value checks only where the recorded run actually used that checkpoint under existing historical decisions; and scan observations, reviews, findings, and verification evidence for unresolved Critical or Important issues.

### Task 6: Run the single clean-room release-audit context

**Files:**
- First-run entry only: `README.md`, then `docs/getting-started.md`, then links reached naturally
- Recovery input only: one selected Work State, artifacts it explicitly references, and canonical docs reachable from them
- Independent review: full frozen repository after both walkthrough reports are complete

**Interfaces:**
- Consumes: repository artifacts only; no prior Chat/Codex conversation or Dogfood narrative.
- Produces: separate first-run, recovery, and release-review reports from one fresh non-authoring context.

- [x] **Step 1: Run the clean-room first-run walkthrough.**

Ask the auditor to simulate a raw bounded software-change request and identify first action, profiling, context source, next phase, developer approvals, valid Approved Handoff, Codex Preflight, implementation/review/verification, and return to developer. Require every point to be classified as clear, ambiguous, contradictory, or hidden-history-dependent.

- [x] **Step 2: Run the artifact-only recovery walkthrough.**

Provide only the selected Work State, its explicit references, and reachable canonical docs. Require current phase, authority, blocker, last satisfied gate, active route, next safe action, and developer-action result.

- [x] **Step 3: Run the independent release review.**

Allow the same clean-room context to inspect the frozen repository and challenge #012-#015 design consistency, human/schema agreement, hidden first-run or recovery history, developer-authority bypass, mandatory-confidence suppression, accidental v0.2 capability, and unsupported release claims.

### Task 7: Audit public/release hygiene and version convention

**Files:**
- Inspect: Git remote, branches, open pull requests, tags, GitHub releases, root tree, `LICENSE`, README, CONTRIBUTING, and release/version-related paths

**Interfaces:**
- Consumes: local Git evidence and observable GitHub state.
- Produces: public hygiene result, existing tags/releases, v0.1.0 existence flags, and a repository-grounded version/release-note convention assessment.

- [x] **Step 1: Confirm shared/public repository state.**

Verify the expected GitHub remote, main as shared/default branch, no open v0.1 pull request, local/origin baseline equality, current tags, observable GitHub releases, and absence or presence of `v0.1.0` tag/release. Do not publish.

- [x] **Step 2: Confirm license and release hygiene.**

Verify Apache-2.0, README/CONTRIBUTING/design agreement, no release-blocking unresolved markers, no accidental package/build/runtime surface, and no unrelated generated or binary artifacts.

- [x] **Step 3: Determine release convention.**

Search for version files, CHANGELOG, release-note directory, tag convention, and release-process documentation. If none exists, record that a later authorized `v0.1.0` Git tag plus GitHub Release body is consistent with current evidence; do not create a convention in Stage 1.

### Task 8: Classify and stop for Chat

**Files:**
- Modify: `docs/superpowers/plans/2026-08-25-v0.1-release-exit.md`
- Do not create: `dogfood/devswitchboard-v0.1-release-exit-016.json`
- Do not modify: `dogfood/README.md`

**Interfaces:**
- Consumes: all audit and independent-review evidence.
- Produces: one release classification, minimum remediation list if applicable, draft release notes in the returned report, and a Chat-owned next action.

- [x] **Step 1: Classify the strongest unresolved finding.**

Return `PASS` only if no release-blocking correction is required; `PASS_WITH_MINIMAL_REMEDIATION_REQUIRED` for a concrete contradiction or hygiene defect correctable within frozen semantics; or `BLOCKED` for new capability, unresolved intent/legal/publication authority, or another non-remediable release requirement.

- [x] **Step 2: Finalize this Stage 1 checkpoint.**

Record the completed audit categories, clean-room results, finding severities, GitHub/tag/release observations, exact verification evidence, proposed minimum remediation, and classification without turning this plan into a new authority artifact.

- [x] **Step 3: Rerun final verification after the checkpoint update.**

Repeat the workflow-integrity suite, complete verifier, every-JSON parse, schema/link/terminology/routing/decision-value/marker checks, exact one-file Stage 1 inventory, plan whitespace validation, and `git diff --check`.

- [x] **Step 4: Stop for Chat evaluation.**

Return phase `verification`, state `READY_FOR_RELEASE_EXIT_EVALUATION`, and next owner `chat`. Do not begin Stage 2 or publication.

## Conditional Stage 2 Boundary

If Chat returns `PASS` and explicit continuation authority, Stage 2 may create only:

- `dogfood/devswitchboard-v0.1-release-exit-016.json`
- an index entry in `dogfood/README.md`
- completion updates to this plan

Any normative correction, release polish, tag, release, or other publication requires the later route/authority Chat and the developer explicitly provide. A `PASS_WITH_MINIMAL_REMEDIATION_REQUIRED` or `BLOCKED` result does not authorize editing the listed correction surfaces.

## Initial Stage 1 Checkpoint

- Worktree: `D:\me\devswitchboard-worktrees\dogfood-016`
- Branch: `codex/dogfood-016-v0.1-release-exit`
- `HEAD`, refreshed `origin/main`, and merge-base: `560f98a04da0807be664bd3c5c28fa282f88e9b8`
- HEAD and origin trees: `99c08f8aa4f35c5a06867d00acfa86beb2a8b97b`
- Preflight outcome: `compatible`
- Relevant local divergence: `false`
- Repository-local `AGENTS.md`: none
- Graphify: no existing graph was available; its required build was not run because it would create prohibited extra artifacts and dispatch extraction agents beyond the single approved read-only release auditor.

## Stage 1 Release-Audit Checkpoint

- Classification: `BLOCKED` pending an explicit contract-version/migration decision for the Approved Handoff authority defect; Stage 1 does not authorize that decision or correction.
- Product boundary: PASS. The tree remains manual-first, documentation-first, advisory, developer-controlled, phase-routed, seven-dimensional, limited to `chat`, `codex`, and `developer`, and mapped only to Superpowers. No CLI, plugin, web app, multi-provider runtime, autonomous enforcement, learned router, package/runtime manifest, generated output, or binary product surface was found.
- Routing and efficiency: PASS. The repository preserves next-incomplete-phase routing, `REMOTE|LOCAL|INTENT`, decision-value stopping, semantic-gate reuse, non-deduplicable final verification, divergence relevance, focused `UNKNOWN` acquisition, profile-refresh route evaluation, the route-and-strategy continuation guard, optional-resource value selection, qualified lower orchestration, mandatory confidence work, and developer authority.
- Contract/schema/example finding (CRITICAL): `schemas/approved-handoff.schema.json` permits `developer_decisions.routing_recommendation_approved: false` even though a ready handoff and readiness evidence require affirmative developer approval. The verifier's general cross-field checks inspect only the canonical handoff example, so an ordinary schema-valid contradictory handoff is possible. Correcting the schema invokes `docs/contracts/README.md`'s authority-semantics version/migration rule; Codex does not choose that migration during the freeze.
- First-run finding (IMPORTANT): the Codex Preflight YAML in `docs/getting-started.md` is artifact-shaped but not schema-conforming: `inspected_baseline` is outside the enum and several required fields are absent. It must become a complete valid example or be explicitly labeled a non-schema excerpt linked to the canonical example.
- Workflow finding (IMPORTANT): `docs/workflows/codex.md` unconditionally requires a repository-grounded implementation plan, while R012, the Superpowers adapter, and Dogfood #012 preserve low-risk direct execution without an implementation-planning phase. The workflow must qualify planning by the selected route and state the bounded detail required for R012 without adding a capability.
- First-run clarification (MINOR): `developer_approval_required: false` in the R012 onboarding example means no additional strategy gate, not no explicit intent/handoff approval; colocated wording would remove first-run ambiguity.
- Contract identity finding (MINOR): Task Profile refresh language relies on the revision of its enclosing task artifact because the Task Profile schema has no standalone `task_id` or `revision`; the human contract should state that embedding rule rather than imply standalone identity.
- Recovery findings (MINOR): the paused #007 Work State fails closed and supports the correct safe action without hidden conversation, but its Revision 1 handoff display name is not a resolvable path and `completed_gates` does not normatively declare chronological order. Historical authority inspection and the literal last satisfied gate are therefore not fully self-addressing.
- Example-index finding (MINOR): `examples/README.md` says nine cross-surface/lifecycle fixtures while eleven such JSON fixtures exist.
- Dogfood #001-#015: PASS. All run records are schema-valid, every recorded finding is remediated, historical checkpoints remain evidence after valid supersession, optional decision-value fields remain backward compatible, #013 and #015 preserve their actual machine-readable checkpoints, and no unresolved Critical or Important finding remains hidden in recorded evidence.
- Public hygiene: repository `Sonos18/devswitchboard` is public; default/shared branch is `main`; no pull request is open; no local or remote tags exist; no GitHub Releases exist; `v0.1.0` tag and release are absent; the license is Apache-2.0.
- Version/release convention: no version file, CHANGELOG, release-notes directory, tag convention, or release-process document exists. A later authorized `v0.1.0` tag plus GitHub Release body is consistent with current evidence, subject to resolution of this audit and separate publication approval.
- Clean-room result: the first-run path and paused recovery safe action are usable without prior Chat/Codex history, but the findings above prevent an unconditional release claim.
- Independent release review: HOLD with one Critical, two Important, and supporting Minor findings; no #012-#015 design contradiction, accidental v0.2 surface, developer-authority override rule, mandatory-confidence suppression, or unresolved historical Dogfood defect was found.
- Initial verification with this plan present: workflow-integrity regressions PASS; complete verifier PASS; 60 JSON files parsed; all schema, link, terminology, routing, decision-value, unresolved-marker, inventory, and whitespace checks PASS.
- First post-checkpoint verification: both canonical Node suites and semantic checks passed; direct plan inspection found the newly written plan lacked a terminal newline. This plan-only formatting defect was corrected before the final fresh rerun.
- Final verification evidence is the fresh post-correction command set reported with the Stage 1 return; no repository content changes follow that run.
- External effects: no commit, push, PR, tag, release, package, Pages action, or other publication occurred.

## Stage 2 Remediation Checkpoint — Revision 2

- Developer decision: `DEC-AH-016-001`, classified `EXISTING_SEMANTICS_CONFORMANCE_BUGFIX`.
- Approved authority result: ready Approved Handoff requires `developer_decisions.routing_recommendation_approved: true`; schema version remains `0.1`; no migration or new semantics are authorized.
- Compatibility safety scan: parsed all tracked JSON and found three `approved_handoff` records. All three use schema version `0.1`, all three record routing approval `true`, and count(false) is zero.
- RED regression: `node tests/verify-regressions.mjs` exited 1 after adding an arbitrary discovered ready Approved Handoff with routing approval `false` while workflow and readiness developer approval remained true.
- Observed RED message: `ready Approved Handoff requires affirmative routing approval: verifier accepted the invalid record`.
- Production/schema remediation had not begun before the compatibility scan and observed RED.
- GREEN regression: after constraining routing approval to `true` and checking every discovered Approved Handoff, `node tests/verify-regressions.mjs` passed and the negative case required both a nonzero verifier exit and the `Approved Handoff authority` diagnostic.
- Focused full verifier: `node scripts/verify.mjs` passed every group after remediation; all three tracked Approved Handoffs remained valid at schema version `0.1`.
- Human conformance: the Approved Handoff contract now states the existing affirmative authority invariant without claiming repository compatibility; the onboarding Codex Preflight example is complete and schema-conforming; R012 direct execution no longer fabricates an implementation-planning phase; the R012 approval label is explained; and the canonical fixture count is eleven.
- Clean-room Revision 2 validation: first-run PASS and representative artifact-only recovery PASS, with no Critical, Important, or Minor finding and no hidden Chat-history dependency.
- Independent Revision 2 release review: PASS with no Critical, Important, or boundary-relevant Minor finding. The reviewer confirmed schema `0.1` conformance enforcement, repository-wide handoff discovery, the RED/GREEN regression boundary, valid onboarding, preserved R006/R012 and #015 resource semantics, developer authority, mandatory confidence work, unchanged recovery, unchanged approved design, and no v0.2 surface.
- Stage 1 deferred observations remain non-blocking and out of scope: #007 display-name addressability, `completed_gates` chronology, and Task Profile enclosing revision identity wording.
- Final release-exit classification after `DEC-AH-016-001`: PASS. PASS is release-exit evidence, not publication.
- Final evidence inventory: exactly the ten developer-approved files, including this plan and Dogfood #016; no protected normative surface or historical Dogfood record changed.
- Final verification gate: workflow-integrity regressions, complete repository verifier, all 61 JSON parses and schema checks, Markdown links, terminology, routing/resource semantics, decision-value semantics, unresolved markers, targeted contradictory-handoff rejection, exact inventory, protected-surface comparison, and Git whitespace checks all pass after the final evidence change.
- Final publication state: no commit, push, PR, tag, GitHub Release, package, Pages action, or other publication occurred.
